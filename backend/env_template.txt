# BigBean Cafe Environment Configuration Template
# Copy this file to .env in the backend directory and update the values

# MySQL Database Configuration (Hostinger)
DB_HOST=localhost
DB_USER=u672910709_bigbeanorg
DB_PASSWORD=Bbc@org26.
DB_NAME=u672910709_bigbeanorg
DB_PORT=3306

# JWT Configuration
JWT_SECRET_KEY=your_jwt_secret_key_here_change_this_in_production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Settings
DEBUG=True
ENVIRONMENT=development

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# Payment Gateway Configuration (Optional)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# File Upload Configuration
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760  # 10MB

# Instructions:
# 1. Copy this file to .env in the backend directory
# 2. Update the JWT_SECRET_KEY with a secure random string
# 3. Configure email settings if you want to enable notifications
# 4. Configure payment gateway settings if needed
# 5. The MySQL credentials are already set for your Hostinger database
